# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: login\successful-login-redirects-to-dashboard.spec.ts >> Sign-in >> successful login redirects to dashboard
- Location: tests\login\successful-login-redirects-to-dashboard.spec.ts:7:7

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /\/dashboard/
Received string:  "http://localhost:3000/login"

Call log:
  - Expect "toHaveURL" with timeout 20000ms
    10 × locator resolved to <html lang="en" class="light">…</html>
       - unexpected value "http://localhost:3000/login"
  - Protocol error (Runtime.callFunctionOn): Internal server error, session closed.

```

```yaml
- link "Skip to main content":
  - /url: "#main"
- main:
  - img "Infoziant"
  - heading "Sign in to iPOMS" [level=1]
  - paragraph: Infoziant Placement Operations Management System
  - button "Sign In"
  - button "Create Account"
  - text: Official Email Address
  - textbox "name@infoziant.com": e2e.success.1787406519818.410842@infoziant.com
  - paragraph: Type your name and press Tab — @infoziant.com is added for you.
  - text: Password
  - button "Forgot password?"
  - textbox "••••••••": TestPass1@2026
  - button "Show"
  - checkbox "Remember this device for 30 days" [checked]
  - text: Remember this device for 30 days
  - button "Authenticating…" [disabled]
- status
- alert
```

# Test source

```ts
  1  | // spec: specs/login.plan.md
  2  | // seed: tests/seed.spec.ts
  3  | import { test, expect } from '../fixtures';
  4  | import { createTestCoordinator } from '../helpers';
  5  | 
  6  | test.describe('Sign-in', () => {
  7  |   test('successful login redirects to dashboard', async ({ page }) => {
  8  |     // 1. Create a fresh test coordinator account via the signup API.
  9  |     const { email, password } = await createTestCoordinator('success');
  10 | 
  11 |     // 2. Type the account's email into "Official Email Address".
  12 |     await page.getByPlaceholder('name@infoziant.com').fill(email);
  13 | 
  14 |     // 3. Type the account's password into "Password".
  15 |     await page.getByPlaceholder('••••••••').fill(password);
  16 | 
  17 |     // 4. Click "Sign-In".
  18 |     await page.getByRole('button', { name: 'Sign-In' }).click();
  19 | 
  20 |     // Generous timeout: first hit on /dashboard in a fresh dev server compiles on demand.
> 21 |     await expect(page).toHaveURL(/\/dashboard/, { timeout: 20000 });
     |                        ^ Error: expect(page).toHaveURL(expected) failed
  22 |     await expect(page.getByRole('heading', { name: /Good (morning|afternoon|evening)/ })).toBeVisible();
  23 |   });
  24 | });
  25 | 
```